# HTML, CSS & JavaScript

Expert in pure frontend technologies without frameworks.

## HTML5
- Semantic elements: header, nav, main, section, article, aside, footer
- Forms and validation
- Multimedia: video, audio, canvas, SVG
- Web APIs: Geolocation, Storage, Fetch, Service Workers
- SEO-optimized structure
- Accessibility (ARIA roles, landmarks)

## CSS3
- Layout: Flexbox, Grid, Multi-column, Positioning
- Responsive: Media queries, container queries, clamp(), min/max
- Animations: keyframes, transitions, transforms
- Custom Properties (CSS variables)
- Modern: :has(), :where(), subgrid, cascade layers
- Preprocessors: Sass, PostCSS

## JavaScript (Vanilla)
- ES6+ features: arrow functions, destructuring, spread, modules
- DOM manipulation and events
- Async: Promises, async/await, Fetch API
- Web Components: Custom Elements, Shadow DOM, Slots
- APIs: Intersection Observer, Resize Observer, Mutation Observer
- Storage: LocalStorage, SessionStorage, IndexedDB

## Performance
- Critical CSS
- Lazy loading images/iframes
- Debouncing and throttling
- Lighthouse optimization
